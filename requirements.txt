pandas>=2.0
numpy>=1.24
scikit-learn>=1.3
joblib>=1.3
matplotlib>=3.7
seaborn>=0.12
streamlit>=1.30
openpyxl>=3.1

# Optional but recommended — the pipeline automatically falls back to
# scikit-learn equivalents if these are not installed:
imbalanced-learn>=0.11   # SMOTE class balancing
lightgbm>=4.0            # LightGBM classifier
shap>=0.44                # SHAP-based explainability
